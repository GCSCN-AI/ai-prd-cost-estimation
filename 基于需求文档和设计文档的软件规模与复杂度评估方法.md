# 从 PRD 与设计文档评估业务流程型软件的规模与复杂度：适合 AI 的方法与可行性验证

> 面向企业管理软件及具有明确业务流程的 B2C/B2B2C 软件早期评估的证据链、计量规则与验证协议

## 摘要

企业管理软件进入采购、立项或投标阶段时，评审人经常只有产品需求文档（Product Requirements Document，PRD）和部分设计材料，却被要求在很短时间内给出规模、复杂度和成本区间。如需引入大模型完成该项工作，虽然可以快速生成“功能点清单”，但生成结果是否来自原文、是否重复计数、换模型后是否稳定，以及谁能对数字负责，仍然没有答案。

本文提出一套适用于企业管理软件及具有明确交易、预约、服务交付、订单履约或活动运营流程的 B2C/B2B2C 软件的 AI 辅助评估方法；纯内容、纯社交、游戏、个人工具和通用工具不在范围内。AI 被限定在证据抽取、候选判断和风险发现位置，主计量方法、权重、系数、公式和最终批准保留给版本化规则与具名评审人。本文还给出一项探索性验证：从 ViBench 评测集的 24 个项目需求文档中，按业务流程门禁确定 10 个评估对象，其中 3 个是混合项目中隔离的业务子系统；其余对象包含企业内部管理软件、B2B 服务软件以及具有明确预约、订单、报价或活动运营流程的 B2C/B2B2C 软件，并按统一口径完成 provisional `US/C/CF` 评估。该结果验证了门禁和计量流程可以从公开 PRD 重放，但尚不构成与独立专家金标准的准确率验证。

**关键词**：企业管理软件；业务流程型软件；B2C/B2B2C；产品需求文档；功能规模；复杂度；Use Case Points；人工智能评估；可行性验证；置信度

## 1. 现状

### 1.1 早期评估面对的是不同问题

采购评审拿到一份 PRD，常见要求是给出“规模、复杂度和费用”。这三个词对应的证据并不相同，混成一个分数后，评审人无法判断数字变化来自需求范围、业务规则、交付条件还是团队生产率。

| 输出 | 要回答的问题 | 需要的证据 |
| --- | --- | --- |
| `US` 原始规模 | 需求包含多少个可交付的功能过程 | 角色、触发条件、业务目标、输入输出、读写和验收条件 |
| `C` 复杂度 | 已确认的规则和技术约束有多难实现 | 状态、权限、事务、一致性、接口和非功能要求 |
| `CF` 不确定性 | 需求或范围还可能改变多少 | 未决问题、版本差异、外部依赖、验收缺口和项目阶段 |
| `D/V` 交付成本 | 这个团队完成交付要付出多少 | 技术栈、复用资产、部署范围、质量门禁、历史人日和成本 |

PRD 通常能够支持 `US` 的候选基线；PRD 加设计文档可以补充 `C` 的约束证据。没有团队、平台和历史数据时，`D` 或 `V` 必须标记为不可评估，不能由模型补一个看似合理的系数。

### 1.2 行业方法已有计量口径，AI 证据仍在形成

IFPUG 的功能点分析和 ISO/IEC 20926 规定了外部输入、外部输出、外部查询、内部逻辑文件和外部接口文件等概念[1][2]；NESMA 方法及 ISO/IEC 24570:2018 提供了另一套功能点计量规则[3]；COSMIC 及 ISO/IEC 19761 以 Entry、Exit、Read、Write 数据移动为核心[4]。这些标准解决“在边界确定后怎样计量”，没有授权模型替评审人决定系统边界或批准结果。

Use Case Points（UCP）源于 Karner 的 *Resource Estimation for Objectory Projects*[5]。Diev 对用例建模和软件估算的讨论指出，用例粒度和复杂度判断会显著影响结果[6]。因此，UCP 适合在需求早期建立相对稳定的候选基线，但不能替代人工边界判断。

将大模型接入功能规模分析仍属于研究和验证阶段。De Vito 等人用 GPT-4 从自然语言用例生成 COSMIC 计量，再由有资质的计量人员复核[7]。这支持“AI 生成候选账本、专业人员批准”的流程方向，不等于形成了无人复核的行业规范。NIST AI RMF Playbook 对有效性、可靠性、验证、审计记录和持续监控的要求[10]，可以作为评估系统治理和放权的参照。

### 1.3 ViBench 提供了稳定的 PRD 测试样本

ViBench 是针对 AI 辅助开发的评测方案，用于评价大语言模型和智能体在软件开发领域的成果和效率，其测试集由 24 个 PRD 构成，每个 PRD 只提供用户目标、角色、业务规则和可观察行为，刻意省略数据模型、API、框架和数据库等实现约束。

该评测方案的汇报论文中报告，自动评测器与人工评审在 1,082 个测试步骤上的 99.07% 一致性。这个数据有效回答了“行为评测器能否复现人工判定”的问题，说明该评测集在从 PRD 到软件的过程中具备较强的稳定性，能够为本文的对比验证提供保障。

## 2. 问题

### 2.1 直接让模型给总数无法审查

模型可能把同一页面的不同角色合并，把定时任务、文件存储或测试夹具误当成 Actor，把示例误认成验收功能，也可能因为提示词或模型版本变化而改变事务拆分。输出一个总数并不能回答“这项来自哪一段原文”“为什么算一次”“哪些内容被排除”。

需求中的未知信息还有另一种风险。模型为了完成回答，往往把没有写出的状态、权限、接口幂等或容量目标默认为简单实现。这样得到的低复杂度既不是观察结果，也没有进入风险清单，后续报价会失去可追溯性。

### 2.2 领域错配会使计算本身失效

规模单位和复杂度驱动依赖软件类型。企业采购、工单、客户、资产、合规记录和运营流程，以及 B2C 的订单、预约、履约和服务流程，主要由业务对象、责任角色、状态和运营约束驱动；个人笔记、白板、游戏、内容站点和开发者工具的主要驱动则可能是交互体验、实时性、渲染、算法或创作自由度。把后者强行转换成业务流程型软件的 UCP 或复杂度分数，会制造可计算但无效的数字。

混合产品也不能凭“有管理员页面”通过。消费者入口、内容浏览和管理后台必须先拆成可独立交付的业务边界；如果 PRD 无法说明管理子系统的业务对象、流程和责任主体，评估应停止。B2C 只有在消费者参与了可观察的交易、预约、服务交付、订单履约或活动运营流程时才适用；单纯浏览、内容消费或社交互动不构成通过依据。

### 2.3 置信度与文档成熟度经常被混为一谈

文档成熟度描述需求处于概念、草稿、结构化 PRD、设计评审还是迭代基线阶段；置信度描述证据覆盖、边界稳定、评审一致、重跑稳定和验证强度。成熟度可以帮助选择 `CF` 的候选区间，不能直接换算成置信度，更不能用 `CF = 1 / Conf` 之类的公式把二者相乘。

### 2.4 研究问题

本文将问题转成四个可证伪的研究问题：

1. `RQ1`：在企业管理及其他业务流程型软件边界内，AI 能否从 PRD 和设计文档识别功能过程，并为每项提供可复核的原文证据？
2. `RQ2`：在计量规则、权重和公式固定后，AI 辅助的 `US` 与 `C` 是否接近独立专家形成的金标准？
3. `RQ3`：适用性门禁、证据状态和确定性重放能否减少跨领域误用，并让结果可复测？
4. `RQ4`：在不降低质量指标的前提下，AI 辅助是否减少评估耗时；哪些证据仍必须由人或原型补齐？

## 3. 方法与假设

### 3.1 适用性门禁

门禁函数记为 `G(p)∈{0,1}`，其中 `p` 是一个独立项目。评估负责人需要从 PRD 或设计材料中找到以下证据：

| 门禁条件 | 通过证据 | 不通过示例 |
| --- | --- | --- |
| `G1` 业务目的 | 服务于组织的经营、管理、运营、合规或对外业务流程；或服务消费者参与的明确交易、预约、服务交付、订单履约或活动运营流程 | 只展示内容、提供个人工具或进行娱乐 |
| `G2` 业务对象与动作 | 存在客户、订单、采购、预约、报价、商品、场地、活动、资产、工单、审计记录等对象，以及创建、提交、审批、确认、取消、履约、查询或变更动作 | 只有静态页面、自由创作、单机玩法或通用编辑 |
| `G3` 参与者与责任 | 至少有两个可识别参与者，或一个消费者角色与一个商家/运营/服务提供方角色；责任、授权或服务交付关系可被定位 | 只有匿名浏览者或单一个人使用，且没有可识别的业务责任关系 |
| `G4` 系统边界 | 可以明确系统边界、排除项和业务/运营责任主体；混合产品可以隔离可独立交付的业务子系统 | PRD 只有平台愿景，无法区分产品、外部服务和实现资产 |
| `G5` 领域适配 | 主要工作量由业务规则、工作流、交易、状态、记录、库存、容量、排班、权限或运营约束驱动 | 纯开发者/通用工具软件、纯内容或社交产品、游戏、媒体/创意应用、嵌入式/实时控制、科学计算、基础设施或平台软件 |

只有 `G1`～`G4` 全部通过且不触发 `G5` 排除项，门禁结果才是 `pass`。B2C 不再因消费者入口而自动排除，但必须满足消费者与商家、运营方或服务提供方之间的可观察业务流程；仅有浏览、内容消费、社交关系或个人记录仍返回 `not applicable`。混合产品必须先隔离可独立交付的业务子系统；无法隔离时返回 `not applicable`。门禁记录保存产品目的、业务参与者或责任主体、系统边界、排除范围、逐项证据、评审人和日期。门禁失败后停止计算，不得输出 `US`、`C`、`CF`、`D`、`V`、`AS` 或 `P`，也不能用“参考值”绕过停止规则。

门禁不是价值判断。一个被称为“工具”的应用，如果实际管理企业预约、工单或采购流程，仍可能通过；一个消费互联网产品，如果核心是订单、预约、履约或商家运营，也可以纳入；但带管理员页面的内容、社交或娱乐产品，不能仅凭管理员角色通过。

适用性判断以“可识别的业务参与者与责任关系”为核心，同时要求业务对象、动作、系统边界和领域驱动证据。B2C 只有在消费者与商家、运营方或服务提供方之间形成可观察的交易或服务流程时才适用；产品面向消费者不会降低可追溯性要求。

### 3.2 输入边界与证据状态

AI 开始抽取前，评估负责人冻结项目边界、输入文件列表、版本或提交号、文档角色、评估阶段和排除项。正式 PRD、设计文档、概念说明、能力清单和测试夹具分别标记；概念说明可以解释背景但不进入计数，测试、图片、CSV、JSON 和种子数据用于验证或实现证据，不直接计为功能点。

每条证据使用四种状态：

- **Observed**：原文直接支持，可定位到文档版本和位置。
- **Assumed**：为了计算采用的明确本地约定，例如把一个定时触发器作为简单 Actor。
- **Unknown**：评估所需但资料没有给出的信息，必须进入待确认清单。
- **Rejected**：看起来相关但被计数边界排除的内容，例如测试条数、页面数量或实现库数量。

AI 不得把 `Unknown` 自动变成零，也不得把 `Assumed` 写成 `Observed`。每个功能过程至少保存 `id`、`source`、`citation`、`actor`、`trigger`、`outcome`、主路径、主要替代路径、数据交互、验收条件、计量单位、候选权重、证据状态和评审决定。

为避免把“支持某项能力”直接当成功能规模，功能过程增加证据等级：

| 等级 | 最低证据 | 计量处理 |
| --- | --- | --- |
| `E0` | 只有能力或页面描述，没有参与者、触发条件和业务结果 | 不进入 `US`，保留为待确认项 |
| `E1` | 有业务参与者、触发条件和业务目标/结果 | 可以进入 provisional UUCP；事务档位中的输入、输出或读写部分必须标为假设 |
| `E2` | 在 `E1` 基础上还有输入/输出、数据交互和验收条件 | 可以按观察证据进入 provisional UUCP |

评估包还应保存“需求条目 → 功能过程 → 设计/数据/接口证据 → 验收条件或测试场景”的追踪矩阵。没有对应功能过程的需求条目进入 `Unknown` 或澄清清单；页面、按钮、API、数据表和测试步骤只作为分解或验证证据，不改变功能过程数量。

### 3.3 主计量方法与确定性公式

方法选择由证据形态决定：

| PRD 或设计已有的证据 | 主方法 | 使用边界 |
| --- | --- | --- |
| Actor、用户目标、用例和事务叙述，缺少字段级细节 | UCP/UUCP | 按用户目标分组，不把每个控件当成用例 |
| ILF、EIF 与 EI、EO、EQ 可逐项识别 | NESMA/IFPUG | 统一采用指示法、估算法或详细法之一 |
| 功能过程明确包含 Entry、Exit、Read、Write | COSMIC | 只计入声明的软件边界内的数据移动 |
| 只有能力条目，没有交互叙述 | provisional UUCP | 事务档位标为假设，置信度保持低 |

一个项目只能选一个主单位，不同单位只能交叉检查，不能相加。本文在缺少技术和环境因子时，采用未调整用例点（provisional UUCP）作为 `US`：简单、平均、复杂用例分别计 `5/10/15`，`US = UUCP = UAW + UUCW`。这里不把未调整用例点称为完整 UCP；完整 UCP 的技术和环境调整因子不在本案例中估计。本方法规定，`UUCW` 只包括 `E1` 和 `E2` 功能过程，`E0` 不计入；`E1` 的事务档位若缺少字段级证据，必须在账本中标为 `Assumed`。模型可以建议 Actor 类型和用例档位，但不能修改档位定义、把 UUCP 与功能点相加，或把测试步骤当成事务。

为使 `UAW` 可重放，本案例沿用 UCP 的 Actor 复杂度约定：人类通过 Web 界面参与的角色按复杂 Actor 计 `3`，文本或协议型外部参与者计 `2`，自动化或简单外部服务计 `1`。同一主体承担多个角色时按业务责任分别记录；仅作为浏览者且不参与业务流程的访问不计 Actor。各项目的角色集合和 `UAW` 在 4.3 的账本中列出。

定义 `C' > 0` 为历史项目的平均复杂度，`D` 为经过批准的交付因素，`V` 为复杂度归一后的单位工作量或单位成本，则：

```text
US = 未调整的主规模单位
AS = US × CF
C  = 0～5 的复杂度指数
P  = AS × (C / C') × D × V
```

只有 `G(p)=1` 时这些量才有定义；门禁失败时所有量均记为 `NA`，而不是零。`AS` 和 `P` 的计算必须从最终证据账本确定性重放。

### 3.4 复杂度模型

复杂度只计已确认的约束。将“已确认的复杂性”和“尚未确认但可能增加工作量的因素”分开记录：前者进入 `C`，后者进入 `CF`，同一证据不得同时计入两者。交易型系统可按以下七个维度从 0 到 5 评分：

| 维度 | 0～1 分 | 2～3 分 | 4～5 分 |
| --- | --- | --- | --- |
| `L` 业务逻辑 | 直接 CRUD 或简单校验 | 条件组合、计算、多种结果 | 规则链、例外密集、难以回退 |
| `W` 流程与状态 | 无状态或单步 | 一个简单状态流 | 严格、不可逆、定时或事件驱动转换 |
| `D` 数据一致性 | 单对象保存 | 两个对象联动 | 跨对象事务、唯一性、幂等、并发控制 |
| `A` 权限与安全 | 一个认证角色 | 角色级访问 | 对象/字段级权限、访客令牌、租户和敏感数据隔离 |
| `I` 外部集成 | 无外部依赖 | 一个稳定接口 | 多系统、重试、限流、签名、异步补偿 |
| `Q` 可靠性与性能 | 普通 Web 要求 | 有容量或响应目标 | 高可用、峰值、离线、实时或严格 SLO |
| `O` 交付与运维 | 单环境 | 测试和基础监控 | 多环境、迁移、灰度、备份恢复和持续运维 |

通用交易型系统的初始权重为 `0.25/0.20/0.15/0.15/0.10/0.10/0.05`，顺序对应 `L/W/D/A/I/Q/O`：

```text
C = 0.25L + 0.20W + 0.15D + 0.15A + 0.10I + 0.10Q + 0.05O
```

每个维度至少需要以下证据之一，或者由评审人明确记录“不适用”。只有当评审人确认该复杂度维度在声明的系统边界内确实不存在，并且没有待确认的外部依赖时，才可将“不适用”按该维度得分 `0` 计入原权重；否则应保留为 `Unknown`，暂不发布完整的 `C`。

| 维度 | 最低证据包 |
| --- | --- |
| `L` | 规则表、计算公式、异常分支或业务决策表 |
| `W` | 状态机、流程图、状态转移条件或定时/事件触发说明 |
| `D` | 数据模型、事务边界、唯一性、幂等或并发要求 |
| `A` | 角色—资源—操作矩阵、租户边界或敏感字段说明 |
| `I` | 接口契约、数据格式、重试/超时/补偿规则 |
| `Q` | 容量、响应时间、可用性、峰值、离线或 SLO 要求 |
| `O` | 环境、部署、迁移、备份、监控、发布或恢复要求 |

这不是国际统一的复杂度标准，而是需要组织校准的经验模型。未知证据不能静默记为低分；可以暂时采用保守假设，但必须写入风险清单。设计或需求已经确认的约束不得再次作为 `CF` 风险加价；未决因素也不能先被假定为 `C` 的高分。同一证据只能进入 `C` 或 `CF` 一次。

### 3.5 AI 的职责边界、风险与验证

“适合”只表示 AI 可以生成候选证据或建议，不表示可以无人审批。

| 参数 | AI 可以做什么 | 主要风险 | 必须怎样验证 |
| --- | --- | --- | --- |
| `US` | 识别 Actor、功能过程、事务和数据移动，给出 `E0/E1/E2` 和引用，发现重复和遗漏 | 合并不同角色、误计示例或测试、提示词变化导致拆分漂移 | 两名评审人先盲评建立金标准，再比较 AI 候选；测 Precision、Recall、F1、引用正确率、重复率和 `US` 偏差 |
| `C` | 从 PRD/设计提取状态、权限、一致性、集成和非功能证据，区分已确认约束与未知项，映射到锚点 | 把“没写”当成“没有”，同一证据在多个维度重复计费，或把未决风险提前算入 `C` | 隐藏模型建议后独立评分，检查七维最低证据包、重复计费和逐维 MAE，再做加权结果敏感性分析 |
| `CF` | 找未决问题、外部依赖、验收缺口、版本变化和责任人，并维护风险登记表 | 压低未知项，或把已确认复杂度再次作为不确定性加价 | 由负责人确认阶段 `M`，按风险登记表 `R` 选择区间；回放历史变更并比较风险召回和区间覆盖率 |
| `D` | 将需求匹配到版本化平台能力、复用资产和交付环境 | 目录过期，把“有组件”误判为“无需集成” | 用小型原型确认原生/扩展/外部服务判断，再比较实际部署、测试、迁移和运维人日 |
| `V` | 清洗历史数据、召回相似项目、解释异常 | 样本偏差、团队和范围混杂、结项信息泄漏、遗漏返工和缺陷 | 使用按时间切分的留出项目，报告 MAE、区间覆盖率和分组误差 |
| 权重、系数、公式、最终报价 | 做敏感性分析、错误提示和计算重放 | 模型以自然语言改写政策或直接承诺报价 | 只能从批准的版本化规则表和统计模型读取，具名负责人批准 |

### 3.6 不确定性、成熟度与置信度

`CF` 反映阶段和未决变化。本文将其写成 `CF = f(M, R)`：`M` 决定基础区间，`R` 是未决风险登记表，负责人根据风险影响在基础区间内选择取值。这里的 `f` 是选区规则，不额外引入未经校准的乘法因子。GB/T 36964-2018 的规模估算条款给出按阶段调整规模的做法[8]；以下区间是组织可以校准的起始政策，不是标准直接规定的成熟度等级：

| 文档成熟度 `M` | 证据状态 | `CF` 候选区间 | 说明 |
| --- | --- | ---: | --- |
| `M0` 概念说明 | 目标和范围仍在讨论 | 1.50～2.00 | 接近预算阶段，不能承诺成本 |
| `M1` PRD 草稿 | 用户目标存在，边界和验收不完整 | 1.50～2.00 | 先关闭高影响未知项 |
| `M2` 结构化 PRD | 功能过程有记录，尚无设计评审 | 1.26～1.50 | 适合建立可复核基线 |
| `M3` 设计评审通过 | 状态、接口、部署和非功能要求已确认 | 1.00～1.25 | 仍需按变更风险取值 |
| `M4` 迭代基线 | 有稳定需求和迭代速度 | 1.00 | 用实际变更和生产率校准 |

`R` 至少覆盖范围和边界、验收条件、外部接口和供应方、数据模型/权限/组织隔离、性能/可用性/合规、版本/迁移/部署六类风险。每条风险保存 `risk_id`、原文引用、影响的 `US` 或 `C` 维度、影响等级、责任人、预计关闭日期，以及是否已经计入 `C`。高影响风险没有责任人或关闭条件时，`CF` 不得取基础区间下界。

为使区间选择可以复核，组织应在版本化规则表中预先定义场景映射：六类风险均已检查且没有开放的高影响风险时取基础区间下段；存在已登记、有人负责且有关闭条件的中高影响风险时取区间中段；存在没有责任人或关闭条件的高影响风险，或风险登记缺失一类以上时取区间上段。若项目无法依据规则归入某一场景，报告完整基础区间并暂停单点 `CF`，不由模型自行插值。

置信度 `Conf` 按参数记录证据覆盖率、假设和未知率、独立评审的边界稳定性、固定配置下的重跑稳定性，以及原型/历史回测等验证强度。各项证据先按照本节门槛归入高、中、低三个序数等级（仅用于排序，不对应概率），再取最弱环节；不把不同量纲的原始指标直接做数值最小值。为避免平均分掩盖单项短板，采用最弱环节原则：

```text
Conf_US = min(证据覆盖、边界稳定、评审一致、重跑稳定、参数验证)
Conf_C  = min(维度证据、锚点校准、评审一致、重复计费检查、重跑稳定)
Conf_CF = min(阶段确认、风险登记完整度、负责人批准、历史回放、C/CF 分离)
```

本文把“中高”作为组织内部的过渡等级，不宣称行业统一含义。`US` 和 `C` 达到中高，至少要求：计数或评分项 100% 有引用、明确假设或批准的不适用判断；`US` 完成功能过程追踪矩阵和 `E0` 排除；`C` 七个维度均有最低证据包并完成重复计费检查；两名评审人盲评后，`US` 相对差异不超过 10%，`C` 逐维绝对差异不超过 0.5；固定配置重跑时 `US` 变化不超过 5%、`C` 变化不超过 0.25。缺少完整历史回测时，可以标为“中高（受限）”，但不能标为高。

`CF` 达到中，至少要求项目负责人确认 `M`，六类风险均完成检查，高影响风险有责任人和关闭条件，`CF` 取值可从版本化规则表重放，并完成至少一轮按时间顺序的需求变更回放。回放覆盖率阈值应由组织设定，启动值可取 80%，不能冒充行业常数。

相对分歧阈值只能作为组织校准点，例如 `US` 相对分歧 10%/20%、`C` 绝对分歧 0.5/1.0，不能冒充行业常数。`AS` 置信度取 `US` 与 `CF` 的较低者；成本 `P` 取 `US/C/D/CF/V` 的最低者，缺少 `D` 或 `V` 时成本置信度为低或不可评估。

### 3.7 方法假设

为使验证可以复现，案例运行预先冻结以下假设：

1. 每个 ViBench `prds` 直接子文件夹是一个独立评估单元，不跨文件夹相加规模或成本。
2. 只有通过业务流程型软件门禁的项目进入计量；不适用项目只作为门禁的负向样本。
3. 10 个通过门禁的项目均采用 provisional UUCP，简单、平均、复杂用例权重为 `5/10/15`，复杂度使用七维默认权重；完整 UCP 的技术和环境调整因子不估算。
4. 项目阶段未在仓库中声明，案例暂按结构化 PRD、设计未评审处理，使用 `CF = 1.26～1.50`。
5. 仓库没有逐项目的已评审设计、团队能力目录、实际人日、成本或生产率，因此 `D` 和 `V` 不估算。
6. Actor 的本地约定是：明确调用远程 AI 或外部服务时按一个简单协议 Actor 计 1 分；浏览器 GPS、内置 CSV/JSON、数据库实现和测试夹具不单独计 Actor。
7. 只有 `E1/E2` 功能过程进入 `US`；无法从现有记录确认等级的条目保留为待复核项，不通过补写文字强行计入。
8. `C` 只使用需求和设计中已确认的约束；未决接口、容量、权限或部署条件进入 `R`，由 `CF` 处理。

## 4. 验证

### 4.1 验证协议

可行性验证不是展示一张完整表格，而是比较“专家独立评估”和“AI 生成证据账本后由专家评审”两条路径。至少两名熟悉 UCP 和业务需求评审的人员独立阅读同一版本 PRD，建立功能过程、Actor、事务档位、七维复杂度证据和未知项清单；第三人或评审会议只处理分歧，并保留原始独立稿。

模型、系统提示词、检索索引、温度、规则版本和人工修改时间必须冻结。项目按零到一创建/功能扩展、规模区间和复杂度区间分层；样本不足时，不应随机切出“看起来很准”的测试集。每个指标报告点估计和不确定区间。

组织可以把以下值作为启动门槛，但必须用结项数据校准。证据等级和双人盲评是 `US/C` 达到中高置信度的必要条件，不是可选的质量加分项：

| 检查项 | 建议起始门槛 | 未通过时的动作 |
| --- | --- | --- |
| 计数项证据覆盖 | 100% 有引用或明确假设 | 阻止提交，退回补证据 |
| 功能过程资格与追踪 | `E1/E2` 计数覆盖 100%；需求—过程—验收追踪 100% | 将 `E0` 移出 `US`，补齐引用或澄清需求 |
| 功能过程识别 | F1 ≥ 0.85，重复率 ≤ 5% | 修正规则、边界或文档切分 |
| 引用正确率 | 抽查和高影响项均 ≥ 95% | 修复检索和引用定位 |
| `US` 双人盲评 | 相对差异 ≤ 10%；固定配置重跑变化 ≤ 5% | 保持人工评审，检查边界和拆分规则 |
| `US` 偏差 | 中位相对误差 ≤ 10%，P90 ≤ 20% | 保持人工评审，不自动发布 |
| `C` 证据完整性 | 七个维度均有最低证据包或批准的不适用判断；重复计费为 0 | 补充设计证据，重新分配 `C/CF` |
| `C` 一致性 | 逐维 MAE ≤ 0.5，P90 差异 ≤ 1.0；加权差异 ≤ 0.5 | 重写锚点，增加设计证据 |
| 重跑稳定性 | 固定配置下 `US` 变化 ≤ 5%，`C` 变化 ≤ 0.25 | 固定解码和规则，排查漂移 |
| `CF` 阶段与风险 | 负责人确认 `M`；六类风险均检查；高影响风险有责任人和关闭条件 | 不得取低端区间，退回补充风险登记 |
| `CF` 历史回放 | 至少一轮时间顺序回放；区间覆盖率达到组织门槛（启动值 80%） | 标记 `CF` 为低，不能进入受控报价 |
| 高影响未知项召回 | ≥ 90% | 增加风险词表和人工检查 |
| 评估耗时 | AI 辅助减少耗时且质量不下降 | 只保留影子或辅助模式 |

### 4.2 ViBench 数据与门禁执行

本次使用 ViBench `main` 分支提交 [`5baa6892bad7dcf6ec0bee4f44e4e46445271f19`](https://github.com/ViBench/vibench-public/tree/5baa6892bad7dcf6ec0bee4f44e4e46445271f19/prds) 的 `prds` 目录[9]。每个直接子文件夹独立阅读；`prd/mvp.txt` 与 `prd/feature*.txt` 组成需求输入，`tests/` 在验证协议中可用于覆盖检查和风险回放，`assets/` 与 `test_assets/` 不直接计功能点。

门禁按第 3.1 节的 `G1`～`G5` 执行。下面的结论是本文分析者依据公开 PRD 作出的案头判定，未取得各项目业务所有者签字，因此只用于方法可行性验证，不构成立项、报价或生产批准。

| 项目文件夹 | 门禁证据摘要 | 结论 | 处理 |
| --- | --- | --- | --- |
| `barber` | 单店员工管理预约、时段和防重复预订 | `pass` | 进入评估 |
| `book_journey` | 多用户阅读记录和公开互动，核心是内容/社交 | `not applicable` | 停止计算 |
| `canary` | 单页问候演示，无业务对象或持久化 | `not applicable` | 停止计算 |
| `collabrative_kaban` | 通用实时协作看板，无可识别业务流程所有者 | `not applicable` | 停止计算 |
| `creative_community` | 可隔离活动报名、容量、出席和支付管理子系统；作品集与社区内容排除 | `pass` | 进入评估（仅计业务子系统） |
| `energy_audit` | 能源审计现场检查、合规规则和审计记录 | `pass` | 进入评估 |
| `family_friendly_venue` | 面向消费者的场所发现和只读筛选 | `not applicable` | 停止计算 |
| `family_social` | 家庭关系和帖子可见性，属于社交产品 | `not applicable` | 停止计算 |
| `fleet_management` | 单用户、离线、本地车辆维护记录工具 | `not applicable` | 停止计算 |
| `furniture_freight` | 企业物流报价、费率卡、报价快照和管理员配置 | `pass` | 进入评估（仅计报价子系统） |
| `hvac` | 客户请求、技师工单、发票和服务运营流程 | `pass` | 进入评估 |
| `language_learning` | 单用户语言练习和内容解锁 | `not applicable` | 停止计算 |
| `logistics` | 可隔离 `/quote` 报价申请与 `/admin/quotes` 运营管理；博客、营销页和通用 ROI 计算器排除 | `pass` | 进入评估（仅计业务子系统） |
| `mafia` | 单房间多人游戏、投票和胜负规则 | `not applicable` | 停止计算 |
| `market_place` | 卖家发布商品、买家下单、订单状态、支付确认和并发占用 | `pass` | 进入评估 |
| `monopoly` | 单浏览器棋盘游戏 | `not applicable` | 停止计算 |
| `notes` | 个人笔记和固定密码门 | `not applicable` | 停止计算 |
| `online_whiteboard` | 通用实时白板和分享链接 | `not applicable` | 停止计算 |
| `pilot_logbook` | Feature 2/3 增加教员签署、不可变背书和机主维护记录；个人日志部分排除 | `pass` | 进入评估（仅计专业背书与维护子系统） |
| `quiz` | 会话制问答游戏和成就 | `not applicable` | 停止计算 |
| `resume_builder` | 单份简历编辑和公开展示工具 | `not applicable` | 停止计算 |
| `slack` | 通用团队聊天，未给出具体组织业务流程边界 | `not applicable` | 停止计算 |
| `srm` | 采购申请、询价、供应商和履约管理 | `pass` | 进入评估 |
| `wedding` | 场地搜索、看团预约、婚礼预订、场地经理审批和日期占用 | `pass` | 进入评估 |

门禁结果为 10 个 `pass`、14 个 `not applicable`。其中 `creative_community`、`furniture_freight` 和 `logistics` 只对可独立交付的业务子系统通过；`pilot_logbook` 只对专业背书与机主维护子系统通过。这不是把不适用项目的规模记为零，而是执行方法的停止规则。

### 4.3 通过门禁项目的规模和复杂度

10 个通过门禁的项目均完成一轮案头 provisional 评估。计数单位是用户目标或功能过程组，不是页面、字段、测试步骤或 Feature 数量；同一项目的 MVP 与 Feature 按版本顺序合并，混合项目只保留 4.2 中声明的业务子系统。下表中的 `S/A/C` 是简单、平均、复杂功能过程数，`UUCW = 5S + 10A + 15C`，`US = UAW + UUCW`。`AS` 按统一的 `CF = 1.26～1.50` 计算并保留两位小数。

| 项目 | Actor（均为 Web 角色） | UAW | 功能过程组（S/A/C） | UUCW | US | C | CF | AS（CF=1.26～1.50） |
| --- | --- | ---: | ---: | ---: | ---: | --- | --- | ---: |
| `barber` | Staff | 3 | 2/2/1 | 45 | 48 | 2.35 | 1.26～1.50 | 60.48～72.00 |
| `energy_audit` | Inspector | 3 | 2/3/3 | 85 | 88 | 3.45 | 1.26～1.50 | 110.88～132.00 |
| `hvac` | Guest、Client、Technician | 9 | 3/5/6 | 155 | 164 | 4.23 | 1.26～1.50 | 206.64～246.00 |
| `srm` | Admin、Buyer、Supplier、SuperAdmin | 12 | 2/5/8 | 180 | 192 | 4.10 | 1.26～1.50 | 241.92～288.00 |
| `creative_community` | Visitor、Member、Admin | 9 | 2/2/8 | 150 | 159 | 3.60 | 1.26～1.50 | 200.34～238.50 |
| `furniture_freight` | Quote user、Admin | 6 | 1/3/6 | 125 | 131 | 3.03 | 1.26～1.50 | 165.06～196.50 |
| `logistics` | Quote requester、Admin | 6 | 1/1/1 | 30 | 36 | 2.70 | 1.26～1.50 | 45.36～54.00 |
| `market_place` | Seller、Buyer | 6 | 2/3/4 | 100 | 106 | 3.88 | 1.26～1.50 | 133.56～159.00 |
| `pilot_logbook` | Pilot、Instructor、Owner、Public verifier | 12 | 1/2/7 | 130 | 142 | 3.88 | 1.26～1.50 | 178.92～213.00 |
| `wedding` | Couple、Venue Manager | 6 | 0/4/7 | 145 | 151 | 3.85 | 1.26～1.50 | 190.26～226.50 |

功能过程组及边界证据如下。括号内的 `S/A/C` 与上表一致；每个组至少对应一个需求文件位置，仍需在正式评估包中补充行级引用和 `E0/E1/E2` 标注。

| 项目 | 计入的功能过程组与证据 | 明确排除或边界决定 |
| --- | --- | --- |
| `barber` | 排班浏览、预约创建/编辑/取消、电话搜索、理发师可用性、重复预约（`mvp.txt`、`feature1–3.txt`；`2/2/1`） | 无认证、支付和消费者自助入口不计 |
| `energy_audit` | 账户/作业、检查表、blower-door、duct leakage、照片、Builder 目录、照片标签、分析（`mvp.txt`、`feature1–3.txt`；`2/3/3`） | 种子校准常量和图片文件不是独立功能过程 |
| `hvac` | 账户/预约、预约转 Job、作业执行、客户 Property/Equipment、发票、RFP/Vendor/PO、重排、经营分析（`mvp.txt`、`feature1–3.txt`；`3/5/6`） | 不把离线支付或营销页面计入；外部 RFP/PO 集成尚未确认 |
| `srm` | 用户/类别/阶段、供应商、采购申请、RFQ、供应商报价、订单、库存、组织与 SuperAdmin、多供应商策略（`mvp.txt`、`feature1–3.txt`；`2/5/8`） | 不把种子数据、看板列数或单个字段当作过程 |
| `creative_community` | 注册/档案、登录、活动发现、邀请码、活动管理、RSVP、My Events/支付状态、出席/缴费、取消、候补加入/退出、候补晋级、用户管理（`mvp.txt`、`feature3.txt`；`2/2/8`） | 作品集、社区帖子和评论属于创意/社交内容，不计入业务子系统 |
| `furniture_freight` | 设置、地点、费率卡、家具目录、报价计算与匹配、报价快照、CSV 原子上传、费率查看、同义词/近似匹配、费率编辑/撤销/导出（`mvp.txt`、`feature1–3.txt`；`1/3/6`） | 无账户、无支付；页面分页和本地存储不独立计数 |
| `logistics` | 报价申请、管理员认证、报价队列查看（`mvp.txt`；`1/1/1`） | 博客、站内搜索、仓库 ROI 工具、联系表单和 AI 内容问答不计入 `/quote` 业务子系统 |
| `market_place` | 发布商品、浏览筛选、详情、结账下单、卖家状态、买家订单页、收藏、邮件查单、离线发布与同步（`mvp.txt`、`feature1–4.txt`；`2/3/4`） | 线下付款不计支付集成；图片是输入数据，不单独计过程 |
| `pilot_logbook` | 角色账户、Lesson Flight、发起签署、教员背书、撤销/重签、公开验证、教员仪表盘、机主飞机、检查项状态、飞行员关联/解除关联（`feature2–3.txt`；`1/2/7`） | 个人普通飞行日志、通用分析和导出只作为专业流程的依赖，不单独计入 |
| `wedding` | 账户、场地管理、场地搜索、详情/可用日历、看团时段、预约看团、审批/拒绝、婚礼预订、任务、Couple—Venue 消息（`mvp.txt`、`feature1–2.txt`；`0/4/7`） | 地理数据 CSV 作为参考输入；地图渲染和视觉效果不单独计数 |

复杂度向量顺序为 `L/W/D/A/I/Q/O`，按第 3.4 节权重计算。由于 10 个项目都只有 PRD/Feature 文档，没有经批准的设计、部署和历史数据，下面的 `C` 是“需求已明确约束”的 provisional 值；未知项进入 `CF` 和风险登记，不被默认为零。

表中的 `C` 点值还使用了显式的本地起始假设：PRD 没有给出部署、运维或容量约束时，按普通 Web、单环境、无严格 SLO 的最低业务场景记为 `Assumed`，而不是 `Observed`。若组织尚未批准这组起始假设，应将相应维度保留为 `Unknown`，发布 `C` 区间或 `C=NA`，不能把表中点值直接用于报价。

| 项目 | `L/W/D/A/I/Q/O` | 主要已确认驱动 | 首要未知或验证点 |
| --- | --- | --- | --- |
| `barber` | 3/3/3/1/1/2/2 | 时段冲突、营业时间、递归预约全有或全无 | 并发保存、持久化和部署恢复 |
| `energy_audit` | 4/3.5/4/3/2.5/3/3 | 测试计算、合规阈值、照片标签、分析有效性 | 校准数据、统计精度、文件存储和备份 |
| `hvac` | 4.5/4.5/4.5/4/4/3.5/3.5 | 预约到作业/发票、角色权限、跟踪链接、RFP/PO | 外部系统契约、访问隔离和容量目标 |
| `srm` | 4.5/4.5/4.5/4/3/3/4 | 采购全链路、RFQ/策略、库存和多租户 | 组织隔离、并发报价、订单生成和迁移 |
| `creative_community` | 4/4.5/4.5/4/1.5/2/1.5 | RSVP 容量、支付状态、取消与候补自动晋级 | 促销并发、线下支付对账、通知与审计 |
| `furniture_freight` | 4.5/3.5/4/1.5/1/2/1.5 | 三层匹配、同义词、原子 CSV、报价快照和撤销 | 本地数据并发、恢复、CSV 大文件和精度边界 |
| `logistics` | 3.5/2.5/2.5/3.5/1.5/2/1.5 | 报价字段校验、管理员会话、报价记录查看 | 存储、PII 保护、通知和生产容量 |
| `market_place` | 4/4.5/4.5/4/2/4/2 | 商品状态、原子下单、不可猜链接、离线同步 | 同步冲突、令牌泄露、缓存失效和恢复 |
| `pilot_logbook` | 4.5/4.5/4.5/4.5/1.5/2.5/2 | 背书快照、撤销重签、检查项状态和角色隔离 | 同设备会话切换、公开链接撤销和删除保留 |
| `wedding` | 4.5/4.5/4.5/4/2/2.5/2 | 日期占用、容量审批、价格计算和消息访问控制 | 实时消息可靠性、地图/地理服务和峰值容量 |

按上述向量加权后，`pilot_logbook` 与 `market_place` 的复杂度约为 3.88，`wedding` 约为 3.85；`logistics` 最低，为 2.70。`creative_community` 的 `US` 为 159，低于 `hvac` 的 164；`srm` 的规模最大，为 192。所有排序都只表示本轮 provisional 账本的相对结果，不能替代第二评审和设计审查。

本轮 10 个项目的参数置信度统一标为：`US` 中低、`C` 中低、`CF` 低至中、`AS` 低至中，`D/V/P` 不可评估。过程组均能回指到项目文件，但尚未达到行级证据、`E0/E1/E2` 全量标注和双人盲评门槛；`CF` 仍使用未经负责人确认的 `M2` 起始区间。对 `creative_community`、`furniture_freight`、`logistics` 和 `pilot_logbook`，还要单独复核子系统边界，边界一旦变化应重新计算 `US`、`C` 和 `AS`。

### 4.4 置信度、限制与受控落地

| 参数 | 本轮状态 | 达标验收条件 | 当前缺口 | 责任主体 |
| --- | --- | --- | --- | --- |
| 门禁 | 案头通过 | `G1`～`G5` 全部有引用；混合项目边界由业务/运营负责人确认；两名评审人结论一致 | 公开材料尚未包含业务负责人签字和独立盲评记录 | 业务/运营负责人、评估负责人 |
| `US` | provisional，中低 | 计数项 100% 有引用或批准假设；`E1/E2` 追踪覆盖 100%；F1 ≥ 0.85；重复率 ≤ 5%；引用正确率 ≥ 95%；双人相对差异 ≤ 10%；固定配置重跑变化 ≤ 5% | 尚无行级证据账本、双人盲评、F1/引用统计和固定配置重跑记录 | 评估负责人、两名独立评审人 |
| `C` | provisional，中低 | 七个维度均有最低证据包或批准的不适用判断；重复计费为 0；逐维 MAE ≤ 0.5；固定配置重跑变化 ≤ 0.25 | 设计、部署和非功能证据不足；尚无盲评、逐维误差和重复计费审计 | 架构/技术负责人、两名独立评审人 |
| `CF` | provisional，低至中 | 项目负责人确认 `M`；六类风险全部检查；高影响风险有责任人和关闭条件；按规则表可重放；至少一轮时间顺序变更回放，区间覆盖率达到组织门槛（启动值 80%） | 阶段未经负责人确认，风险登记和历史回放尚未完成 | 项目负责人、风险负责人、评估负责人 |
| `D` | 不可评估 | 建立版本化平台能力目录，并用关键原型确认原生/扩展/外部服务判断；与实际部署、测试、迁移和运维人日对照 | 缺少技术栈、复用资产、部署范围、质量门禁和能力目录 | 技术负责人、交付负责人 |
| `V` | 不可评估 | 建立统一规模口径的历史项目库，按时间切分回测并报告 MAE、区间覆盖率和分组误差 | 缺少实际人日、成本、生产率、返工/缺陷和 AI 使用数据 | PMO/数据负责人、财务负责人 |
| `AS` | provisional，低至中 | 仅在 `US` 达到中高且 `CF` 达到中后发布；置信度取两者短板 | `US` 和 `CF` 尚未达到发布门槛 | 评估负责人、项目负责人 |
| `P` | 不可评估 | `US/C/D/CF/V` 均完成验证并通过成本模型回测后才可输出 | 缺少经验证的 `D` 和 `V`，不生成费用或报价 | 交付负责人、财务负责人 |

ViBench 的 PRD 省略实现技术约束，不能证明模型能正确判断部署拓扑、容量、高可用、迁移和团队复用；测试计划偏重终端功能正确性，也不能证明视觉质量、性能或长期运维。通过门禁的 10 个项目来自同一评测集，不能代表所有行业、监管强度或交付组织；其中 3 个是混合产品中的隔离子系统，边界判定对最终规模影响较大。功能过程的 `E0/E1/E2`、需求追踪、第二评审和 `C/CF` 分离尚未完成，当前数字仍是 provisional 基线。混合项目的子系统边界、Feature 与 MVP 的版本关系、评估集进入提示词或检索索引、专家金标准由一人产生，都会降低结论等级。

一次可审查的评估包至少保存输入文件及版本、门禁证据、边界和排除项、结构化证据账本、主计量方法、规则和权重版本、模型和提示词版本、检索索引版本、原始模型输出、人工修改、未决问题、批准人和时间戳。系统应阻止无引用计数、无阶段和风险依据的 `CF`、无历史回测的 `V`，并要求公式能从账本重新计算。

落地分三阶段。影子模式只生成账本并维护人工基线；辅助模式允许预填清单和风险，但评审人逐项确认；受控生产模式只有在达到组织门槛、完成领域验证并配置回滚和审计后，才允许结果进入报价流程，最终承诺仍由具名负责人批准。模型、提示词、规则、平台版本、外部接口、权限模型、部署范围或验收条件变化时，必须重新评估。

### 4.5 评估集边界与可复用性

本文只把能够取得独立项目级 PRD、明确参与者、业务对象、系统边界和可观察业务结果的条目纳入项目级评估。只有网页操作步骤、单个功能任务、参数变体或数据集级场景说明的公开条目，可以用于解析器和门禁的辅助验证，但不计入项目清单，也不得与完整项目相加。若要扩展样本，必须为每个来源保存固定版本的原始需求、设计或领域说明、许可状态和逐项证据，并通过同一门禁复核。

### 4.6 最终进入清单与可计量范围

按上述规则，最终通过门禁的是 10 个独立评估对象，其中 3 个是混合项目中可独立交付的业务子系统。清单冻结如下；ViBench 提交号用于复现，条目级证据位置见 4.2。

| 编号 | 来源 | 评估单元 | 单元类型 | 门禁 | 当前可计量范围 |
| ---: | --- | --- | --- | --- | --- |
| 1 | ViBench | `barber` | 完整 PRD 项目 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 2 | ViBench | `energy_audit` | 完整 PRD 项目 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 3 | ViBench | `hvac` | 完整 PRD 项目 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 4 | ViBench | `srm` | 完整 PRD 项目 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 5 | ViBench | `creative_community` | 混合项目中的业务子系统 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 6 | ViBench | `furniture_freight` | B2B 报价子系统 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 7 | ViBench | `logistics` | 混合项目中的业务子系统 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 8 | ViBench | `market_place` | B2C 交易项目 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 9 | ViBench | `pilot_logbook` | 专业服务子系统 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |
| 10 | ViBench | `wedding` | B2C 预约与预订项目 | `pass` | provisional `US/C/CF/AS`；`P` 不可评估 |

因此，本轮可以严谨地说“ViBench 中有 10 个独立评估对象通过业务流程门禁，并完成一轮 provisional `US/C/CF/AS` 评估”。这些结果仍不是 AI 准确率、正式预算或报价：10 个评估对象都需要补齐行级证据、`E0/E1/E2`、第二评审、设计证据和负责人确认；由于缺少经验证的 `D/V`，10 个评估对象的 `P` 均不可评估。不同项目的功能过程和参数变体不得跨项目相加，也不得把混合项目中被排除的内容、社交或营销功能回填到规模中。

## 5. 参考文献

[1] International Function Point Users Group (IFPUG). *Function Point Analysis*. https://ifpug.org/ifpug-standards/fpa

[2] ISO/IEC 20926:2009. *Software and systems engineering — Software measurement — IFPUG functional size measurement method 2009*. International Organization for Standardization. https://www.iso.org/standard/51717.html

[3] ISO/IEC 24570:2018. *Software engineering — NESMA functional size measurement method version 2.1 — Definitions and counting guidelines for the application of function point analysis*. International Organization for Standardization. https://www.iso.org/standard/68917.html

[4] ISO/IEC 19761:2011. *Software engineering — COSMIC: a functional size measurement method*. International Organization for Standardization. https://www.iso.org/standard/54849.html

[5] Karner, G. *Resource Estimation for Objectory Projects*. Objective Systems SF AB, 1993.

[6] Diev, S. “Use cases modeling and software estimation.” *ACM SIGSOFT Software Engineering Notes*, 31(6), 1–4, 2006. https://doi.org/10.1145/1218776.1218780

[7] De Vito, G., Di Martino, S., Ferrucci, F., Gravino, C., and Palomba, F. “LLM-Based Automation of COSMIC Functional Size Measurement From Use Cases.” *IEEE Transactions on Software Engineering*, 51(5), 1500–1523, 2025. https://doi.org/10.1109/TSE.2025.3554562

[8] GB/T 36964-2018. 《软件工程 软件开发成本度量规范》. 国家市场监督管理总局、国家标准化管理委员会，2018. https://www.hebjyt.cn/hbcms/ewebeditor/uploadfile/20210922120234687.pdf

[9] Zhong, P., Vaezipoor, P., Cui, F., et al. “ViBench: A Benchmark on Vibe Coding.” *Proceedings of the ACM Conference on AI and Agentic Systems (ACM CAIS ’26)*, 497–513, 2026. https://doi.org/10.1145/3786335.3813162

[10] National Institute of Standards and Technology (NIST). *AI Risk Management Framework Playbook*. https://airc.nist.gov/airmf-resources/playbook/

## 6. 结论

在企业管理软件及其他业务流程型软件场景中，AI 可以把 PRD 和设计材料转换成带引用的、分级的功能过程，识别已确认的复杂度证据，并维护未决风险清单；它适合缩短检索、整理和候选判断时间。主计量单位、边界、权重、阶段系数、组织生产率和最终报价必须由确定性规则、历史数据和具名评审人负责。

适用性门禁是方法的一部分，而不是报告末尾的免责声明。ViBench 的 24 个项目需求文档中，10 个评估对象满足企业管理或明确业务流程型 B2C/B2B2C 软件的 `G1`～`G5` 条件，其中包括活动运营、报价、订单、预约、专业背书和服务履约等流程；其余项目因缺少适用的业务边界或触发排除条件而停止计量。本轮对这 10 个评估对象均完成一轮统一的 provisional `US/C/CF/AS` 评估，结果可以从 Actor、功能过程组、七维向量和公式重放；但由于行级证据、第二评审、设计审查、风险负责人确认尚未完成，不能把这些数字解释为已验证金标准。缺少设计、交付和历史成本数据时，`D/V` 与 `P` 仍不可评估。ViBench 的行为评测器一致性结果不能替代规模评估的专家金标准。

置信度要按参数管理。文档成熟度 `M` 只帮助选择 `CF` 基础区间，风险登记表 `R` 决定区间内取值；二者都不能直接折算为置信度。`US` 以 `E1/E2` 功能过程、需求追踪矩阵和双人盲评为基础，`C` 只使用有最低证据包的已确认约束，未决因素进入 `CF`。`US` 和 `C` 可以在完成这些检查、但尚缺完整历史回测时标为“中高（受限）”；`CF` 在阶段已确认、风险登记完整并通过一次历史回放后标为“中”。`AS` 取 `US` 与 `CF` 的短板，成本 `P` 取 `US/C/D/CF/V` 的短板。没有 `D` 或 `V` 时，任何费用数字都不应进入正式报价。

> **建议**
>
> 先在影子模式中冻结输入、执行业务流程型软件门禁、建立带引用的证据账本和独立专家金标准，再用回测结果决定是否放权。对于只有任务级操作步骤的公开夹具，必须先完成项目边界和 mini-PRD 归一化，不得把参数化任务变体当作多个项目或直接相加。每次输入、边界、规则或交付条件变化，都保留相邻版本的评估差异并重新评审；门禁不通过或关键证据缺失时，明确输出 `not applicable` 或“不可评估”，不要用一个看似精确的数字掩盖方法失效。
